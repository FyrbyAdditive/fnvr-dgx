# benchmark

First-run capacity wizard. Spins up N synthetic RTSP streams against the local pipeline, measures GPU / NVDEC / NVENC saturation, and surfaces a recommended camera count for the detected power mode.

Lands in M6. Stub only at M1.
