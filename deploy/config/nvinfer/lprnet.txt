# LPRNet — NVIDIA TAO licence-plate OCR. Runs as tertiary nvinfer on
# plate crops from LPDNet (gie-id=2). DeepStream surfaces the decoded
# plate text as NvDsClassifierMeta; the InferSrcProbe walks
# obj_meta->classifier_meta_list->label_info_list to read it.
#
# Reference config: NVIDIA-AI-IOT/deepstream_tao_apps — lpr_tao5 sample,
# paths adapted to fnvr layout. CTC decoder lives in the TAO parser .so
# compiled in Dockerfile.pipeline.

[property]
gpu-id=0
net-scale-factor=0.00392156862745098
onnx-file=/var/lib/fnvr/models/anpr/LPRNet_usa.onnx
# Match nvinfer's auto-derived name so subsequent boots reuse the
# built engine.
model-engine-file=/var/lib/fnvr/models/anpr/LPRNet_usa.onnx_b16_gpu0_fp16.engine
labelfile-path=/etc/fnvr/nvinfer/lprnet.labels
batch-size=16
network-mode=2
gie-unique-id=3
operate-on-gie-id=2
operate-on-class-ids=0
process-mode=2
# Classifier (not detector). The output is the plate string, decoded
# by the TAO custom parser.
network-type=1
num-detected-classes=3
model-color-format=0
classifier-async-mode=0
classifier-threshold=0.35

parse-classifier-func-name=NvDsInferParseCustomNVPlate
custom-lib-path=/opt/nvidia/deepstream/deepstream/lib/libnvdsinfer_custom_impl_lpr.so
