# DeepStream nvinfer — TrafficCamNet (INT8 on GPU). Source assets are
# seeded from the DeepStream-L4T image onto the fnvr-data volume by
# deploy/docker/pipeline-entrypoint.sh on first boot. nvinfer compiles
# the TRT engine next to onnx-file using its own derived filename; once
# on the persistent volume the compiled engine survives container
# recreate (saves 60-90s per worker on every redeploy).
#
# model-engine-file below points at that derived name so subsequent runs
# hit the deserialise path instead of re-compiling.
#
# Classes: car (0), bicycle (1), person (2), road_sign (3).

[property]
gpu-id=0
net-scale-factor=0.00392156862745098
onnx-file=/var/lib/fnvr/models/primary_detector/resnet18_trafficcamnet_pruned.onnx
model-engine-file=/var/lib/fnvr/models/primary_detector/resnet18_trafficcamnet_pruned.onnx_b1_gpu0_int8.engine
int8-calib-file=/var/lib/fnvr/models/primary_detector/cal_trt.bin
labelfile-path=/var/lib/fnvr/models/primary_detector/labels.txt
batch-size=1
process-mode=1
model-color-format=0
# 0=FP32  1=INT8  2=FP16
network-mode=1
num-detected-classes=4
interval=0
gie-unique-id=1
# 1=DBSCAN 2=NMS 3=Hybrid 4=None
cluster-mode=2

[class-attrs-all]
topk=20
nms-iou-threshold=0.5
pre-cluster-threshold=0.2

[class-attrs-0]
topk=20
nms-iou-threshold=0.5
pre-cluster-threshold=0.4
