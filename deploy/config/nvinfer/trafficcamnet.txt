# DeepStream nvinfer — TrafficCamNet (INT8 on GPU). Bundled in the
# deepstream-l4t samples image, so works out of the box on first boot
# (TRT compiles the .engine on first run; takes a couple of minutes).
#
# Classes: car (0), bicycle (1), person (2), road_sign (3).

[property]
gpu-id=0
net-scale-factor=0.00392156862745098
onnx-file=/opt/nvidia/deepstream/deepstream/samples/models/Primary_Detector/resnet18_trafficcamnet_pruned.onnx
model-engine-file=/var/lib/fnvr/models/engines/trafficcamnet_int8.engine
int8-calib-file=/opt/nvidia/deepstream/deepstream/samples/models/Primary_Detector/cal_trt.bin
labelfile-path=/opt/nvidia/deepstream/deepstream/samples/models/Primary_Detector/labels.txt
batch-size=1
process-mode=1
model-color-format=0
# 0=FP32  1=INT8  2=FP16
network-mode=1
num-detected-classes=4
interval=0
gie-unique-id=1
# 1=DBSCAN 2=NMS 3=Hybrid 4=None
cluster-mode=2

[class-attrs-all]
topk=20
nms-iou-threshold=0.5
pre-cluster-threshold=0.2

[class-attrs-0]
topk=20
nms-iou-threshold=0.5
pre-cluster-threshold=0.4
