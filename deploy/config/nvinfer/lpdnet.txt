# LPDNet — NVIDIA TAO licence-plate detector, runs as SGIE on vehicle
# detections from the primary YOLO26. Weights compiled on first boot to
# /var/lib/fnvr/models/anpr/LPDNet_usa.engine (persistent volume).
#
# Reference config: NVIDIA-AI-IOT/deepstream_tao_apps — apps/tao_others/
# deepstream_lpr_app/lpd_us_onnx.txt, paths adapted to fnvr layout.

[property]
gpu-id=0
net-scale-factor=0.0039215697906911373
onnx-file=/opt/fnvr/anpr/LPDNet_usa.onnx
model-engine-file=/var/lib/fnvr/models/anpr/LPDNet_usa.engine
labelfile-path=/etc/fnvr/nvinfer/lpdnet.labels
batch-size=16
# FP16. INT8 LPDNet calibration not distributed; stay FP16 for now.
network-mode=2
num-detected-classes=1
interval=0
# Unique across the nvinfer chain — pgie=1 (yolo26), this=2, lprnet=3.
gie-unique-id=2
operate-on-gie-id=1
# COCO class IDs the LPDNet should look inside of. car=2, motorcycle=3,
# bus=5, truck=7. (YOLO26 is trained on COCO; we mirror the ids.)
operate-on-class-ids=2;3;5;7
# 2 = SECONDARY (classifier mode for this GIE); runs on crops of
# upstream-detected vehicle bboxes rather than the full frame.
process-mode=2
cluster-mode=2
model-color-format=0
maintain-aspect-ratio=1
output-blob-names=output_bbox/BiasAdd:0;output_cov/Sigmoid:0
# Required for LPDNet — the ONNX ships with DBSCAN-friendly output
# naming that the generic nvinfer parser can handle.

[class-attrs-all]
pre-cluster-threshold=0.2
group-threshold=1
eps=0.2
minBoxes=3
