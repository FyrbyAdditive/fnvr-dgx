# Architecture

See [PLAN.md](../../PLAN.md) for the full architecture. Per-topic deep-dives will live here as they're written.

Planned docs:

- `pipeline.md` — DeepStream pipeline construction, zero-copy NVMM, DLA offload.
- `rules-engine.md` — rule DAG, cross-camera correlation, incident threading.
- `storage.md` — segment layout, tiering, purge, quota, SMART.
- `training-loop.md` — active learning, dataset versioning, shadow deployment, drift.
- `federation.md` — hub / edge-node protocol, event replication, on-demand video pull.
